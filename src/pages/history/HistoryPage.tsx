import classNames from 'classnames/bind';
import styles from './HistoryPage.module.scss';

const cx = classNames.bind(styles);

export const HistoryPage = () => {
  return <div className={cx('')}>history page</div>;
};
