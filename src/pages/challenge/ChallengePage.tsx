import classNames from 'classnames/bind';
import styles from './ChallengePage.module.scss';

const cx = classNames.bind(styles);

export const ChallengePage = () => {
  return <div className={cx('')}>challenge page</div>;
};
